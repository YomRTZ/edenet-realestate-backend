enum PropertyStatus {
  PENDING
  MINTED
  DECLINED
}

enum DocumentFileType {
  IMAGE
  DOCUMENT
}

enum RequestType {
  MINT
  UPDATE
}

enum RequestStatus {
  PENDING
  APPROVED
  DECLINED
}
enum Role {
  Government
  Citizen
}